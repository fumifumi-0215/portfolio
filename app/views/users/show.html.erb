<% provide(:title, @user.name) %>

  <div class="col-md-8">
    <% if @user.items.any? %>
      <h3>投稿 (<%= @user.items.count %>)</h3>
      <ol class="items">
        <%= render @items %>
      </ol>
      <%= will_paginate @items %>
    <% end %>
  </div>  
</div>
